## What does this PR do?

<!-- e.g. "Adds Design Chess solution", "Fixes Splitwise rounding bug", "Improves elevator scheduling section" -->

## Checklist

- [ ] Follows the 7-part solution template (requirements → entities → design decisions → Mermaid diagram → runnable Java → pitfalls → follow-ups), if adding a solution
- [ ] Java code is inside a single ```java block and ends with a public `Main` demo that finishes quickly
- [ ] Index tables updated in both `solutions/README.md` and the main `README.md` (if adding a solution)
- [ ] CI is green — the workflow extracts every ```java block, compiles with `-Werror`, and runs every `Main`

## Related issues

<!-- e.g. "Closes #12" -->
